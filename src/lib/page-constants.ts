/**
 * Shared constants.
 * Only text that is truly identical across all pages.
 */

/**
 * Standard empty state description - identical across all pages
 * Content editors: This text appears when no search results are found
 */
export const EMPTY_STATE_DESCRIPTION = 'Try adjusting your search or filter criteria to find what you\'re looking for.';

/**
 * Standard empty state action button text - identical across all pages
 * Content editors: This is the button text that clears all filters
 */
export const EMPTY_STATE_ACTION = 'Clear all filters';